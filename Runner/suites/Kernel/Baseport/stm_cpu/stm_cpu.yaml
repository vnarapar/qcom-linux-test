metadata:
    format: Lava-Test Test Definition 1.0
    name: STM
    description: "This test case validates the System Trace Macrocell (STM) functionality"
    maintainer:
        - vnarapar@qti.qualcomm.com
    os:
        - openembedded
    scope:
        - functional
    devices:
        - rb3gen2
        - ridesx
        - lemans evk
        - monaco evk

run:
    steps:
        - cd Runner
        - $PWD/suites/Kernel/Baseport/stm_cpu/run.sh || true
        - $PWD/utils/send-to-lava.sh $PWD/suites/Kernel/Baseport/stm_cpu/stm_cpu.res || true
