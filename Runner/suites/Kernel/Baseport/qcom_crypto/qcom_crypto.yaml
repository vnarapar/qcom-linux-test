metadata:
    format: Lava-Test Test Definition 1.0
    name: Qcom_Crypto
    description: "Validates the basic functionality of cryptographic operations using the kcapi tool"
    maintainer:
        - vnarapar@qti.qualcomm.com
    os:
        - openembedded
    scope:
        - functional
    devices:
        - rb3gen2
        - ridesx
        - lemans evk
        - monaco evk

run:
    steps:
        - cd Runner
        - $PWD/suites/Kernel/Baseport/qcom_crypto/run.sh || true
        - $PWD/utils/send-to-lava.sh $PWD/suites/Kernel/Baseport/qcom_crypto/qcom_crypto.res || true
