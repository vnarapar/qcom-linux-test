metadata:
  name: qcrypto
  format: "Lava-Test Test Definition 1.0"
  description: "Test KCAPI basic features"
  os:
    - linux
  scope:
    - functional

run:
  steps:
    - REPO_PATH=$PWD
    - cd Runner/suites/Kernel/Baseport/qcrypto
    - ./run.sh || true
    - $REPO_PATH/Runner/utils/send-to-lava.sh qcrypto.res || true

