metadata:
    format: Lava-Test Test Definition 1.0
    name: CPU Frequency test
    description: "Test validates the CPU frequency scaling capabilities of a system using the Linux `cpufreq` subsystem."
    maintainer:
        - vnarapar@qti.qualcomm.com
    os:
        - openembedded
    scope:
        - functional
    devices:
        - rb3gen2
        - ridesx
        - lemans evk
        - monaco evk

run:
    steps:
        - cd Runner
        - $PWD/suites/Kernel/Baseport/CPUFreq_Validation/run.sh || true
        - $PWD/utils/send-to-lava.sh $PWD/suites/Kernel/Baseport/CPUFreq_Validation/CPUFreq_Validation.res || true
