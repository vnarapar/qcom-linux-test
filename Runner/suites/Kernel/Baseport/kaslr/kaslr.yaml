metadata:
  name: kaslr
  format: "Lava-Test Test Definition 1.0"
  description: "Test basic kaslr features"
  os:
    - linux
  scope:
    - functional

run:
  steps:
    - REPO_PATH=$PWD
    - cd Runner/suites/Kernel/Baseport/kaslr
    - ./run.sh || true
    - $REPO_PATH/Runner/utils/send-to-lava.sh kaslr.res || true
