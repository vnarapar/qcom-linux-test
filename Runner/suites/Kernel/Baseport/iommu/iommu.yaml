metadata:
    format: Lava-Test Test Definition 1.0
    name: IOMMU Test
    description: "This test validates the IOMMU (Input-Output Memory Management Unit) support and runtime status"
    maintainer:
        - vnarapar@qti.qualcomm.com
    os:
        - openembedded
    scope:
        - functional
    devices:
        - rb3gen2
        - ridesx
        - lemans evk
        - monaco evk

run:
    steps:
        - cd Runner
        - $PWD/suites/Kernel/Baseport/iommu/run.sh || true
        - $PWD/utils/send-to-lava.sh $PWD/suites/Kernel/Baseport/iommu/iommu.res || true
