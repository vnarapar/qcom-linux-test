metadata:
  name: iommu
  format: "Lava-Test Test Definition 1.0"
  description: "Test basic IOMMU features"
  os:
    - linux
  scope:
    - functional

run:
  steps:
    - REPO_PATH=$PWD
    - cd Runner/suites/Kernel/Baseport/iommu
    - ./run.sh || true
    - $REPO_PATH/Runner/utils/send-to-lava.sh iommu.res || true

