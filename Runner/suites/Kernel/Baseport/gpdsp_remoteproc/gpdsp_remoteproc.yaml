metadata:
    format: Lava-Test Test Definition 1.0
    name: GPDSP remoteproc test
    description: "This test performs the start/stop operations for gpdsp remoteproc"
    maintainer:
        - vnarapar@qti.qualcomm.com
    os:
        - openembedded
    scope:
        - functional
    devices:
        - rb3gen2
        - ridesx
        - lemans evk
        - monaco evk

run:
    steps:
        - cd Runner
        - $PWD/suites/Kernel/Baseport/gpdsp_remoteproc/run.sh || true
        - $PWD/utils/send-to-lava.sh $PWD/suites/Kernel/Baseport/gpdsp_remoteproc/gpdsp_remoteproc.res || true
