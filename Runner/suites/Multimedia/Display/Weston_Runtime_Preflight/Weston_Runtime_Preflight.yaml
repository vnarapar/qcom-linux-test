metadata:
  name: weston-runtime-preflight
  format: "Lava-Test Test Definition 1.0"
  description: "Validate the existing Weston and Wayland runtime before Weston client tests."
  os:
    - linux
  scope:
    - functional

params:
  WAIT_SECS: "10"
  VALIDATE_EGLINFO: "1"

run:
  steps:
    - REPO_PATH=$PWD
    - cd Runner/suites/Multimedia/Display/Weston_Runtime_Preflight
    - WAIT_SECS="${WAIT_SECS}" VALIDATE_EGLINFO="${VALIDATE_EGLINFO}" ./run.sh || true
    - $REPO_PATH/Runner/utils/send-to-lava.sh Weston_Runtime_Preflight.res || true
